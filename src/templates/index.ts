import DefaultPageTemplate from "./default-page";
import DefaultPostTemplate from "./default-post";

const templates = {
  page: {
    default: DefaultPageTemplate,
  },
  post: {
    default: DefaultPostTemplate,
  },
};

export default templates;
